plugins { id("com.android.application") version "9.1.1" }
java {
    toolchain.languageVersion.set(JavaLanguageVersion.of([JAVA_VERSION]))
}
android {
    namespace = "[GAME_PACKAGE].android"
    compileSdk = 36
    defaultConfig {
        applicationId = "[GAME_PACKAGE]"
        minSdk = [ANDROID_MIN_SDK]
        targetSdk = 36
        versionCode = 1
        versionName = "1.0.0"
        ndk { abiFilters += listOf("arm64-v8a", "x86_64") }
    }
    compileOptions {
        sourceCompatibility = JavaVersion.toVersion([JAVA_VERSION])
        targetCompatibility = JavaVersion.toVersion([JAVA_VERSION])
        isCoreLibraryDesugaringEnabled = true
    }
    sourceSets["main"].assets.srcDir("../app/src/main/resources")
    packaging { resources.excludes += "META-INF/DEPENDENCIES" }
}
dependencies {
    implementation(platform("org.jetbrains.kotlin:kotlin-bom:1.8.22"))
    implementation(project(":app"))
    implementation("org.jmonkeyengine:jme3-android:[JME_VERSION]")
    implementation("androidx.fragment:fragment:1.8.9")
    coreLibraryDesugaring("com.android.tools:desugar_jdk_libs:2.1.5")
}
